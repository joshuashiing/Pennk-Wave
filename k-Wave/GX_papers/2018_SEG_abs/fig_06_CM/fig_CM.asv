clear;

n_PML = 20;
load('BP_cut_model_gas_chimney');
c = velp;
h = dx;
scale = 2e2;
clim = [min(c(:)) max(c(:))];

load('case01');
p1 = p; d1 = d;
plot_model_snap(c, p1, n_PML, h, scale, clim, 1, 411);

load('case02');
p2 = p; d2 = d;
plot_model_snap(c, p2, n_PML, h, scale, clim, 1, 412);

load('case03');
p3 = p; d3 = d;
plot_model_snap(c, p3, n_PML, h, scale, clim, 1, 413);

dp = p2 - p3;
plot_model_snap(c, dp, n_PML, h, scale, clim, 1, 414);


plot_gather(d, 